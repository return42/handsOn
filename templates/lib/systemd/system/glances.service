[Service]

[Unit]
Description=Glances

[Service]
User = glances
Group = glances
ExecStart=/home/glances/py3/bin/glances -1 --tree --enable-process-extended --fs-free-space --diskio-iops -t 10 -w --bind 0.0.0.0
Restart=on-abort

[Install]
WantedBy=multi-user.target
